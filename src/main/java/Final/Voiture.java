package Final;

// Tentative d'héritage d'une classe finale (ceci générera une erreur)
class Voiture extends Vehicule {
    public Voiture(String modele) {
        super(modele);
    }

    // Le fait d'étendre une classe finale génère une erreur de compilation


}
